import { moderateSize } from '../styles/moderateSize';

// Inner header height (the visible row with back-button / title / icons)
export const HEADER_INNER_HEIGHT = moderateSize(50);

// Content area border radius (rounded top corners)
export const CONTENT_BORDER_RADIUS = moderateSize(30);

// Standard content padding used by "main" content style
export const CONTENT_PADDING = moderateSize(16);

// Extra offset added to booking screens (larger header visual area)
export const BOOKING_EXTRA_OFFSET = moderateSize(30);
